version: '3'

services:
  db:
    image: postgres
  api:
    build:
      context: ./services/api
      dockerfile: Dockerfile
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./services/api:/code
    ports:
      - "8000:8000"
    depends_on:
      - db